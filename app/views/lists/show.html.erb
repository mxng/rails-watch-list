<div class="container">
  <h1><%= @list.name %></h1>

  <div class="row">
    <div class="card-deck">
      <%= render @bookmarks %>
    </div>
  </div>

  <%= link_to "Add bookmark", new_list_bookmark_path(@list), class: "btn btn-primary m-3" %>
</div>
